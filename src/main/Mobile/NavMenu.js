import React, { useState } from "react";
import Navigation from "./Navigation";
import styles from "./NavMenu.module.css";

const NavMenu = (props) => {
  return <div className={styles["nav-menu-wrapper"]}></div>;
};

export default NavMenu;
